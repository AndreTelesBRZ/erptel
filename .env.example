# Django
SECRET_KEY=defina-uma-chave-secreta-segura
DEBUG=1
ALLOWED_HOSTS=localhost,127.0.0.1
TIME_ZONE=America/Sao_Paulo

# Banco de dados (usado pelo docker-compose)
DB_ENGINE=postgres
POSTGRES_DB=erptel
POSTGRES_USER=postgres
POSTGRES_PASSWORD=minhasenha
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Outros (configure conforme necessidade)
DATA_UPLOAD_MAX_NUMBER_FIELDS=20000
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=ERP <no-reply@example.com>
# SEFAZ_API_BASE_URL=
# SEFAZ_API_TOKEN=
# Token opcional usado para proteger a API de integração de pedidos
# APP_INTEGRATION_TOKEN=troque-este-token

# Sincronização de produtos (intervalo em segundos; mínimo 60s)
PRODUCT_SYNC_INTERVAL_SECONDS=180
